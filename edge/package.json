{
  "name": "gundemai-edge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --import tsx --test test/**/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260723.1",
    "@types/node": "^24.1.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2",
    "wrangler": "^4.113.0"
  },
  "overrides": {
    "sharp": "^0.35.3"
  }
}
